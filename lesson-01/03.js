/* Используйте цикл while для нахождения суммы всех нечётных чисел от 1 до 20.
  Результат вычислений сохраните в переменной sum.

  Вам понадобиться оператор остатка от деления %. Например, выражение 5 % 2 вернёт остаток 1.
*/

// your code
let sum = 0;       // Переменная для хранения суммы
let i = 1;         // Начинаем с числа 1

while (i <= 20) {  // Пока i меньше или равно 20
    if (i % 2 !== 0) {   // Проверяем, является ли число нечётным (остаток от деления на 2 не равен 0)
        sum += i;          // Добавляем нечётное число к сумме
    }
    i++;                // Увеличиваем i на 1
}

console.log(sum);  // В