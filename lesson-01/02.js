/* С помощью цикла while найдите факториал числа 10 и присвойте его переменной factorial
  Справка:
  Факториал числа 𝑛 (обозначается как 𝑛!)  — это произведение всех натуральных чисел от 1 до 𝑛 включительно.
  Например, факториал числа 3 это 1 * 2 * 3 (6).
*/

// your code
let n = 10;          // Число, для которого считаем факториал
let factorial = 1;   // Начальное значение для факториала (1, так как умножаем)
let i = 1;           // Начинаем с 1

// Цикл while будет выполняться, пока i <= n
while (i <= n) {
    factorial *= i;    // Умножаем текущий факториал на i
    i++;               // Увеличиваем i на 1
}

console.log(factorial);